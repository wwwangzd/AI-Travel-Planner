# AI 旅行规划师后端部署与使用指南

本文档详细说明如何部署和使用 AI 旅行规划师后端服务。

---

## 📋 目录

1. [前置要求](#前置要求)
2. [外部服务配置](#外部服务配置)
3. [本地开发环境部署](#本地开发环境部署)
4. [Docker 部署](#docker-部署)
5. [测试验证](#测试验证)
6. [常见问题](#常见问题)

---

## 前置要求

### 软件环境
- Node.js 18+ 
- npm 或 yarn
- Docker & Docker Compose (如果使用 Docker 部署)

### 检查环境
```bash
node --version  # 应该 >= 18.0.0
npm --version
docker --version  # 可选
```

---

## 外部服务配置

本项目依赖以下外部服务，需要提前注册并获取 API 密钥。

### 1. Supabase（数据库）

**注册步骤**：

1. 访问 https://supabase.com
2. 点击 "Start your project" 注册账号
3. 创建新项目：
   - 填写项目名称
   - 设置数据库密码（请牢记）
   - 选择地区（建议选择离你最近的）
4. 等待项目初始化完成（约 2 分钟）

**获取配置信息**：

1. 进入项目后，点击左侧菜单 "Settings" -> "API"
2. 复制以下信息：
   - `Project URL` → 对应 `SUPABASE_URL`
   - `service_role` key → 对应 `SUPABASE_SERVICE_ROLE_KEY`

**初始化数据库表**：

1. 点击左侧菜单 "SQL Editor"
2. 点击 "New query"
3. 复制 `backend/src/database/schema.sql` 文件的全部内容
4. 粘贴到编辑器中
5. 点击 "Run" 执行 SQL
6. 确认所有表创建成功（左侧 Table Editor 会显示新建的表）

---

### 2. LLM API（大语言模型）

#### 选项 A: 阿里云通义千问（推荐国内用户）

**注册步骤**：

1. 访问 https://dashscope.aliyun.com/
2. 登录阿里云账号（没有则需注册）
3. 开通 DashScope 服务
4. 进入控制台，点击 "API-KEY 管理"
5. 创建新的 API-KEY 并复制

**配置**：
```bash
LLM_API_KEY=sk-xxxxx  # 你的通义千问 API Key
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus
```

#### 选项 B: OpenAI

**注册步骤**：

1. 访问 https://platform.openai.com/
2. 注册并登录账号
3. 进入 API Keys 页面
4. 创建新的 API Key 并复制

**配置**：
```bash
LLM_API_KEY=sk-xxxxx  # 你的 OpenAI API Key
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4
```

#### 选项 C: 其他兼容 OpenAI API 的服务

如 DeepSeek、智谱AI 等，按照各自平台的说明获取 API Key 和 Base URL。

---

### 3. 高德地图 API

**注册步骤**：

1. 访问 https://lbs.amap.com/
2. 注册并登录高德开放平台
3. 进入控制台 -> 应用管理 -> 我的应用
4. 点击 "创建新应用"
   - 应用名称：AI 旅行规划师
   - 应用类型：Web 服务
5. 创建后，点击 "添加 Key"
   - Key 名称：backend
   - 服务平台：Web 服务
   - 提交
6. 复制生成的 Key

**配置**：
```bash
AMAP_KEY=你的高德地图Key
AMAP_SECRET=你的高德地图Secret（如果有）
```

---

### 4. 科大讯飞语音识别

**注册步骤**：

1. 访问 https://www.xfyun.cn/
2. 注册并登录账号
3. 进入控制台
4. 点击 "语音听写（流式版）"
5. 创建应用：
   - 应用名称：AI 旅行规划师
   - 应用平台：WebAPI
6. 创建后，在应用列表中查看应用详情
7. 复制以下信息：
   - APPID
   - APISecret
   - APIKey

**配置**：
```bash
XF_APP_ID=你的APPID
XF_API_KEY=你的APIKey
XF_API_SECRET=你的APISecret
```

---

## 本地开发环境部署

### 步骤 1: 克隆项目

```bash
cd AI-Travel-Planner/backend
```

### 步骤 2: 安装依赖

```bash
npm install
```

### 步骤 3: 配置环境变量

1. 复制环境变量模板：
```bash
cp .env.example .env
```

2. 编辑 `.env` 文件，填入你的配置：
```bash
# 使用你喜欢的编辑器打开
nano .env
# 或
vim .env
# 或
code .env
```

3. **生成 JWT 密钥**（推荐使用以下任一方法）：

方法 1 - 使用 OpenSSL：
```bash
openssl rand -base64 64
```

方法 2 - 使用 Node.js：
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
```

方法 3 - 在线生成器：
访问 https://www.uuidgenerator.net/dev-tools/random-string-generator 生成 64 位随机字符串

4. 完整的 `.env` 配置示例：
```bash
# Server
PORT=3000
NODE_ENV=development

# Supabase
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# JWT（使用上面生成的随机字符串）
JWT_SECRET=你生成的随机字符串
JWT_EXPIRES_IN=7d

# 科大讯飞语音识别
XF_APP_ID=你的APPID
XF_API_KEY=你的APIKey
XF_API_SECRET=你的APISecret

# 高德地图
AMAP_KEY=你的高德Key

# LLM API (通义千问)
LLM_API_KEY=sk-你的通义千问Key
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus
```

**配置说明**：
- `JWT_SECRET`: 必须替换为自己生成的随机字符串（见步骤3）
- `SUPABASE_SERVICE_ROLE_KEY`: 从 Supabase 获取（见外部服务配置）
- `XF_*`: 从科大讯飞控制台获取（见外部服务配置）
- `AMAP_KEY`: 从高德地图控制台获取（见外部服务配置）
- `LLM_API_KEY`: 从阿里云 DashScope 获取（见外部服务配置）

**配置检查清单**：
```bash
# 在启动前，确保以下配置都已正确填写
✅ SUPABASE_URL - 已从 Supabase 复制
✅ SUPABASE_SERVICE_ROLE_KEY - 已从 Supabase 复制（注意是 service_role 不是 anon）
✅ JWT_SECRET - 已生成随机字符串（不要使用默认值）
✅ XF_APP_ID, XF_API_KEY, XF_API_SECRET - 已从科大讯飞获取
✅ AMAP_KEY - 已从高德地图获取
✅ LLM_API_KEY - 已从阿里云通义千问获取
✅ LLM_BASE_URL - 确认为通义千问地址
✅ LLM_MODEL - 确认为 qwen-plus
```

### 步骤 4: 启动开发服务器

```bash
npm run dev
```

你应该看到类似输出：
```
Server is running on port 3000
Environment: development
Health check: http://localhost:3000/health
```

### 步骤 5: 验证服务

打开浏览器访问：http://localhost:3000/health

应该看到：
```json
{
  "success": true,
  "message": "AI Travel Planner API is running",
  "timestamp": "2025-11-01T..."
}
```

---

## Docker 部署

### 步骤 1: 配置环境变量

在 `backend` 目录创建 `.env` 文件（同上）

### 步骤 2: 构建 Docker 镜像

```bash
# 在项目根目录执行
docker-compose build
```

### 步骤 3: 启动容器

```bash
docker-compose up -d
```

### 步骤 4: 查看日志

```bash
docker-compose logs -f backend
```

### 步骤 5: 验证服务

```bash
curl http://localhost:3000/health
```

### 其他 Docker 命令

```bash
# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 查看运行状态
docker-compose ps

# 进入容器
docker-compose exec backend sh
```

---

## 测试验证

### 1. 使用 curl 测试

**健康检查**：
```bash
curl http://localhost:3000/health
```

**用户注册**：
```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "123456",
    "username": "测试用户"
  }'
```

**用户登录**：
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "123456"
  }'
```

保存返回的 token，用于后续请求。

**生成旅行计划** (需要替换 YOUR_TOKEN)：
```bash
curl -X POST http://localhost:3000/api/plans/generate \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "destination": "日本",
    "startDate": "2025-12-01",
    "endDate": "2025-12-05",
    "budget": 10000,
    "travelersCount": 2,
    "preferences": {
      "interests": ["美食", "动漫"]
    }
  }'
```

### 2. 使用 Apifox 测试

1. 导入 `API测试文档.md` 中的接口
2. 配置环境变量：
   - `base_url`: http://localhost:3000
3. 按照文档中的测试流程进行测试

### 3. 使用 Postman 测试

类似 Apifox，导入接口配置并测试。

---

## 常见问题

### Q1: npm install 失败

**解决方案**：
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### Q2: Supabase 连接失败

**检查项**：
1. 确认 `SUPABASE_URL` 和 `SUPABASE_ANON_KEY` 配置正确
2. 确认 Supabase 项目状态为 Active
3. 检查网络连接
4. 查看详细错误日志

### Q3: LLM API 调用超时

**解决方案**：
1. 检查 API Key 是否正确
2. 确认账户余额充足
3. 如果使用 OpenAI，确认网络可以访问
4. 可以尝试使用国内的 LLM 服务（如通义千问）

### Q4: 数据库表未创建

**解决方案**：
1. 重新在 Supabase SQL Editor 中执行 `schema.sql`
2. 检查是否有错误提示
3. 确认所有表都已创建（在左侧 Table Editor 中查看）

### Q5: Docker 构建失败

**解决方案**：
```bash
# 清理 Docker 缓存
docker system prune -a

# 重新构建
docker-compose build --no-cache
```

### Q6: 端口 3000 被占用

**解决方案**：

方法 1: 修改端口
```bash
# 在 .env 文件中修改
PORT=3001
```

方法 2: 停止占用端口的进程
```bash
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# Windows (PowerShell)
Get-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess | Stop-Process
```

### Q7: 语音识别失败

**检查项**：
1. 确认科大讯飞 API 配置正确
2. 音频文件格式是否支持
3. 音频文件大小是否超过限制（10MB）
4. 查看科大讯飞控制台的调用统计

### Q8: TypeScript 编译错误

**解决方案**：
```bash
# 清理构建产物
rm -rf dist

# 重新构建
npm run build
```

---

## 性能优化建议

1. **使用生产模式**：
   ```bash
   NODE_ENV=production npm start
   ```

2. **启用缓存**（可选，需要 Redis）

3. **使用 PM2 管理进程**：
   ```bash
   npm install -g pm2
   pm2 start dist/index.js --name ai-travel-planner
   pm2 save
   pm2 startup
   ```

---

## 安全建议

1. **生产环境必须修改 JWT_SECRET**
2. **妥善保管 SUPABASE_SERVICE_ROLE_KEY**
   - ⚠️ **绝对不要**将此 key 暴露给前端或客户端
   - ⚠️ **绝对不要**将包含此 key 的 .env 文件提交到 Git
   - ✅ 此 key 只能在后端服务器使用
   - ✅ service_role key 拥有完全的数据库访问权限
3. **不要将 .env 文件提交到 Git**
4. **定期更新依赖包**
5. **使用 HTTPS**
6. **限制 API 请求频率**

---

## 下一步

- 查看 `API测试文档.md` 了解所有接口
- 开始开发前端应用
- 配置生产环境部署

---

## 技术支持

如遇到问题，请：
1. 查看控制台日志
2. 检查 `.env` 配置
3. 查阅本文档的常见问题部分
4. 查看各外部服务的官方文档

祝使用愉快！🚀
